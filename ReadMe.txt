#author: Hongyi Zhang(andrew id: hongyiz)

Usage:
	python dataOrganizer.py filename > outputfile
the reason that I print the result to the screen rather than directly write it to a file is that
we can easily use redirection to redirect it to a file, while print can provide more flexibility for
showing the result.